@model IEnumerable<MVC.Models.DegreeRequirement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Degree)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Requirement)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Degree.DegreeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Requirement.RequirementId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DegreeRequirementId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DegreeRequirementId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DegreeRequirementId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
